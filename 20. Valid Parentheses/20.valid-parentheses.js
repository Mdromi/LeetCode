/**
 * node 20.valid-parentheses
 * @param {string} 
 * @return {boolean}
 */
 var isValid = function(s) {

    for(let i = 0 ; i < s.length; i++) {
        console.log(s[i]);
    }

    // console.log(s.length);
};

const s = "()[]{}"
isValid(s)